{
  "buildCommand": "cp prisma/schema.postgresql.prisma prisma/schema.prisma && prisma generate && prisma db push --accept-data-loss && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"]
}
